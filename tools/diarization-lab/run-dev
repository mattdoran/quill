#!/bin/sh
set -eu

tool_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export QUILL_DIARIZATION_LAB_HOME="$tool_root/devdata"
export UV_CACHE_DIR="$tool_root/devdata/cache/uv"
cd "$tool_root"
exec uv run python -m app serve "$@"
